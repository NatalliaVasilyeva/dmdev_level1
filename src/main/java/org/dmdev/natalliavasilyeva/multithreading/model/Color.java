package org.dmdev.natalliavasilyeva.multithreading.model;

import java.util.Arrays;
import java.util.List;

public enum Color {
    RED,
    WHITE;

    public static final List<Color> COLOR_VALUES = Arrays.asList(values());

}